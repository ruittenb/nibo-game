
 - fix landing gear of ship
 - central pick up icon in nav-panel
 - animated items still below darkness mask: plan 'flying-coins' (css stacking context problem)
 - burn disease with torch?

 - gold eating NPC / requiring payment

 - dit soort commentaar is natuurlijk te stom voor woorden:
   z-index: var(--z-endgame-overlay);  /* z-index: 2000 */
   color: #ff0000;  /* Red text */
   background: #000000d9;  /* Semi-transparent: black with ~85% opacity */

 - Deze klopt niet: er is geen left arrow from pos 7
   Poison (S1-L1-P6)   │ 2          │ From P5→P6 (right arrow), from P7→P6 (left arrow)

 - serviceworker?
    // sw.js
    self.addEventListener('fetch', (e) => e.respondWith(fetch(e.request)));
    // main.js
    navigator.serviceWorker?.register('/sw.js');

